[project]
name = "invariants"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ujaan Das", email = "ujaandas03@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "accelerate>=1.14.0",
    "bitsandbytes>=0.49.2",
    "pydantic>=2.13.4",
    "pytest>=9.1.1",
    "tokenizers>=0.22.2",
    "torch>=2.12.1",
    "transformers>=5.12.1",
]

[build-system]
requires = ["uv_build>=0.11.2,<0.12.0"]
build-backend = "uv_build"